import { SafeAreaView } from 'react-native-safe-area-context';

import Root from './root';

const App = () => {
  return (
    <SafeAreaView>
      <Root />
    </SafeAreaView>
  );
};

export default App;
